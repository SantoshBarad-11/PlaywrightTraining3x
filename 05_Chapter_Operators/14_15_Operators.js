// Assignment operator
let x = 10; // Assigns the value 10 to the variable x

// Arithmetic operators
let y = 5;
let z = 3;
let sum = y + z; // Adds y and z
let difference = y - z; // Subtracts z from y
let product = y * z; // Multiplies y and z
let quotient = y / z; // Divides y by z

// Modulus operator
let remainder = y % z; // Gets the remainder of y divided by z //

console.log("Sum:", sum);
console.log("Quotient:", quotient);

//Exponentiation operator
let exponent = y ** z;      
console.log("Exponent:", exponent);

let x1 = 10;
x1 += 5;
console.log("x1:", x1);
x1= x1 - 3;
console.log("x1 after subtraction:", x1);